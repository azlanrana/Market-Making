[package]
name = "mm-metrics"
version = "0.1.0"
edition = "2021"

[dependencies]
mm-core-types = { path = "../core" }
mm-portfolio = { path = "../portfolio" }
rust_decimal = { workspace = true }
rust_decimal_macros = { workspace = true }
chrono = { workspace = true }
